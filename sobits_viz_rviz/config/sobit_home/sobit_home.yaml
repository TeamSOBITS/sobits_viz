# SOBIT HOME in RViz2: which displays the generated config shows. The
# cameras and lidars it names come from sobits_viz_robots
# config/sobit_home/sobit_home.robot.yaml. Regenerate after editing:
#   ros2 run sobits_viz_rviz make_config --params config/sobit_home/sobit_home.yaml
/**:
  ros__parameters:
    app_id: "sobit_home"

    # SOBIT HOME publishes bare frame names; RViz does no stripping, so a
    # robot whose driver prefixes its frames sets this instead.
    frame_prefix: ""

    views:
      scene:
        name: "Scene"
        enable: true
        fixed_frame: "odom"
        camera_distance_m: 4.0
        camera_height_m: 0.6
        show_collision: false
        urdf: true
        grid: true
        tf: false
        scans: true
        description_topic: "robot_description"

      cameras:
        head_camera:
          name: "Head"
          enable: true
          color:
            enable: true
            use_compressed: true
          depth:
            enable: true
            # RViz decodes 16-bit PNG compressedDepth only, and the
            # simulated camera sends 32FC1, so depth is shown raw.
            use_compressed: false
            range_m: 10.0
        hand_left_camera:
          name: "Hand Left"
          enable: true
          color:
            enable: true
            use_compressed: true
        hand_right_camera:
          name: "Hand Right"
          enable: true
          color:
            enable: true
            use_compressed: true

      lidars:
        lidar_front:
          enable: true
          point_size_px: 3.0
          color: [255, 0, 0]
        lidar_back:
          enable: true
          point_size_px: 3.0
          color: [0, 0, 255]

      base:
        name: "Base"
        enable: true
